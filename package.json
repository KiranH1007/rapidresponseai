{
  "name": "rapid-response-ai",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@google/genai": "^1.29.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "serve": "^14.2.3"
  },
  "scripts": {
    "start": "serve -s build",
    "start": "serve -s .",
    "build": "echo 'No build step configured for this project structure.' && exit 0"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}